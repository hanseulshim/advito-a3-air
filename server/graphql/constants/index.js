const {
  CONTRACT,
  PRICING_TERM,
  TARGET_TERM,
  DISCOUNT,
  NOTE
} = require('./contract');
const { DATA_SET } = require('./dataSet');
const { PROCESS } = require('./process');
const { PROJECT } = require('./project');

module.exports = {
  CONTRACT,
  PRICING_TERM,
  TARGET_TERM,
  DISCOUNT,
  NOTE,
  DATA_SET,
  PROCESS,
  PROJECT
};
