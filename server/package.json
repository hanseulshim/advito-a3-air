{
  "name": "advito-air-a3-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "sls offline",
    "alpha-server": "sls deploy -s alpha",
    "alpha-client": "aws s3 sync ../dist s3://alpha.boostlabs/air/ --acl public-read",
    "beta-server": "sls deploy -s beta",
    "beta-client": "aws s3 sync ../dist s3://beta.boostlabs/air/ --acl public-read",
    "dev-server": "sls deploy -s dev",
    "dev-client": "aws s3 sync ../dist s3://dev.boostlabs/air/ --acl public-read",
    "test": "sls deploy -s test"
  },
  "license": "MIT",
  "dependencies": {
    "apollo-server-lambda": "^2.4.0",
    "aws-sdk": "^2.413.0",
    "graphql": "^14.1.1",
    "lodash.merge": "^4.6.1"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-import": "^2.16.0",
    "eslint-plugin-node": "^8.0.1",
    "eslint-plugin-promise": "^4.0.1",
    "eslint-plugin-standard": "^4.0.0",
    "serverless-offline": "^4.4.2",
    "serverless-plugin-warmup": "^4.3.3-rc.1"
  }
}
